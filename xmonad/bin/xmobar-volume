#!/bin/bash

if [[ $1 == "up" ]]; then
    pactl set-sink-volume 0 +1%
    pactl set-sink-volume 1 +1%
elif [[ $1 == "down" ]]; then
    pactl set-sink-volume 0 -1%
    pactl set-sink-volume 1 -1%
else
    amixer get Master|grep %|cut -d \[ -f 2|cut -f 1 -d \]
fi
